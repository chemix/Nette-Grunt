{**
 * My Application layout template.
 *
 * @param string   $basePath  web base path
 * @param string   $robots    tell robots how to index the content of a page (optional)
 * @param array    $flashes   flash messages
 * @param string   $version   app version string for media files
 * @param boolean  $develMode is app in devel mode ?
 *}

<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="description" content="Nette Framework web application skeleton">
	<meta name="robots" content="{$robots}" n:ifset="$robots">

	<title>{#title|striptags|upper}Nette Application Skeleton{/#}</title>

	{if $develMode}
		<!-- build:css {$basePath}/css/screen.min.css -->
		<link rel="stylesheet" media="screen,projection,tv" href="{$basePath}/css/screen.css">
		<!-- endbuild -->
	{else}
		<link rel="stylesheet" media="screen,projection,tv" href="{$basePath}/css/screen.min.css?{$version}">
	{/if}

	{if $develMode}
		<!-- build:css {$basePath}/css/print.min.css -->
		<link rel="stylesheet" media="print" href="{$basePath}/css/print.css">
		<!-- endbuild -->
	{else}
		<link rel="stylesheet" media="screen,projection,tv" href="{$basePath}/css/print.min.css?{$version}">
	{/if}

	<link rel="shortcut icon" href="{$basePath}/favicon.ico">

	<script src="http://code.jquery.com/jquery-1.8.1.min.js"></script>

	{if $develMode}
		<!-- build:js {$basePath}/js/app.min.js -->
		<script src="{$basePath}/js/netteForms.js"></script>
		<!-- endbuild -->
	{else}
		<script src="{$basePath}/js/app.min.js?{$version}"></script>
	{/if}
	{#head}{/#}
</head>

<body>
	<script> document.body.className+=' js' </script>

	<div n:foreach="$flashes as $flash" class="flash {$flash->type}">{$flash->message}</div>

	{include #content}
</body>
</html>
